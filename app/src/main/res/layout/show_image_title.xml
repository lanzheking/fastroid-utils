<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="wrap_content"
    android:background="#1a2121"
    android:gravity="center" >

    <RelativeLayout
        android:id="@+id/title_layout"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content" >

            <!-- 左按钮 -->
            <Button
                android:id="@+id/title_left_btn"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_alignParentLeft="true"
                android:layout_centerVertical="true"
                android:gravity="center"
                android:paddingBottom="2dp"
                android:paddingLeft="10dp"
                android:paddingRight="10dp"
                android:paddingTop="2dp"
                android:singleLine="true"
                android:background="@null"
	            android:visibility="gone"
                android:textSize="@dimen/show_image_title_btntext"
                android:textColor="@color/title_btn_text_color" />

            <!-- 右按钮 -->
            <Button
                android:id="@+id/title_right_btn"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_alignParentRight="true"
                android:layout_centerVertical="true"
                android:gravity="center"
	            android:visibility="gone"
                android:paddingBottom="2dp"
                android:paddingLeft="10dp"
                android:paddingRight="10dp"
                android:paddingTop="2dp"
                android:background="@null"
                android:textSize="@dimen/show_image_title_btntext"
                android:textColor="#ffffff" />

        <!-- 中间部分布局：宽度fill两边 -->
        <RelativeLayout
            android:layout_width="fill_parent"
            android:layout_height="fill_parent"
            android:layout_centerVertical="true"
            android:background="@null"
            android:layout_toLeftOf="@id/title_right_btn"
            android:layout_toRightOf="@id/title_left_btn"
            android:gravity="center" >
            <!-- 中间标题 -->
            <TextView
                android:id="@+id/title_middle_tv"
                android:layout_width="wrap_content"
                android:layout_height="fill_parent"
                android:layout_centerInParent="true"
                android:gravity="center"
                android:text="选择图片"
                android:textSize="@dimen/show_image_title_btntext"
                android:textColor="#ffffff" />
            <!-- 中间搜索框 -->
        </RelativeLayout>
    </RelativeLayout>
    

</RelativeLayout>